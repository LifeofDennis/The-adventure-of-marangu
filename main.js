const navToggle=document.querySelector('.nav-toggle');const navMenu=document.querySelector('.nav-menu');if(navToggle&&navMenu){navToggle.addEventListener('click',()=>{const open=navMenu.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}
document.querySelectorAll('[data-tab]').forEach(button=>{button.addEventListener('click',()=>{const group=button.closest('.tabs-section');if(!group)return;group.querySelectorAll('[data-tab]').forEach(b=>b.classList.remove('active'));group.querySelectorAll('.tab-panel').forEach(p=>p.classList.remove('active'));button.classList.add('active');const panel=group.querySelector('#'+button.dataset.tab);if(panel)panel.classList.add('active');});});
const form=document.querySelector('[data-contact-form]');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'Guest';const subject=encodeURIComponent(data.get('subject')||'Tour inquiry');const body=encodeURIComponent(`Hello Adventure of Kilimanjaro,%0D%0A%0D%0AMy name is ${name}.%0D%0A%0D%0A${data.get('message')||''}%0D%0A%0D%0AReply to: ${data.get('email')||''}`);window.location.href=`mailto:info@theadventureofkilimanjaro.com?subject=${subject}&body=${body}`;});}
